<!DOCTYPE html>
<html>
<body>

<h1>Blog</h1>
<p>My post.</p>

<ul>
    <% posts.forEach(function(post){ %>
        <li>
        <h3><a href="<%= post.id %>"><%= post.title %></a></h3>
        </li>
    <% }) %>
</ul>

</body>
</html>